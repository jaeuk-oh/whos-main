# Open Questions

## ax-portfolio-redesign - 2026-05-26

- [ ] Canonical contact email — is `jaeuk.oh@whatonsolve.com` correct, or should it be a personal Gmail? — Affects Contact CTA `mailto:` target and trust signal.
- [ ] 2 vs 3 project cards in hero — spec says "2~3개"; placeholder defaults to 3 cards. — Affects above-the-fold density on smaller laptops (e.g., 1366×768).
- [ ] LinkedIn URL inclusion — placeholder is empty (omitted). — Affects whether a 3rd contact link slot appears.
- [ ] Real GitHub repo URLs and live demo URLs for at least 2 projects — required for AC2 to fully pass in production (placeholders ship in the plan, real values must replace them before public deploy).
- [ ] Confirm `position: fixed` footer is fully retired in favor of static contact section — spec implies yes, but worth one-line confirm before deletion.
